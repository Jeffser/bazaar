using Gtk 4.0;
using Adw 1;

template $BzDecoratedScreenshot: Adw.Bin {
    child: $BzScreenshot screenshot_widget {
      paintable: bind template.async-texture;
    };

    Gtk.GestureClick click_gesture {
      pressed => $open_dialog(template);
    }
}
